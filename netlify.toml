[build]
  command = "bun install && bun run vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  BUN_VERSION = "1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
